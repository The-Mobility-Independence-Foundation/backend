// I don't think this is currently needed but I'm leaving it here for now, can delete later.

export interface Inventory {
    inventoryID: number, 
    parentInventoryID: number, 
    organizationID: number, 
    name: string, 
    description: string, 
    location: string
}
